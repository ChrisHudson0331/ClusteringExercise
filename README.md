# ClusteringExercise
Visualization of the result of a program which clustered 10k public opinion survey answers generated by ChatGPT

# Installation
Open a terminal in this folder and run:
python -m pip install -r requirements.txt

# Run
python hierarchy_visualizer.py

Then open the local address printed in the terminal, normally:
http://127.0.0.1:8050
